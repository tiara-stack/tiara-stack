apiVersion: apps/v1
kind: Deployment
metadata:
  name: sheet-apis
  labels:
    app: sheet-apis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sheet-apis
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: sheet-apis
    spec:
      containers:
        - name: sheet-apis
          image: <IMAGE>
          ports:
            - containerPort: 3000
              name: sheet-apis-svc
            - containerPort: 9464
              name: sheet-apis-met
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: SHEET_INGRESS_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: sheet-apis-secret
                  key: otelExporterOtlpEndpoint
            - name: ZERO_CACHE_SERVER
              valueFrom:
                secretKeyRef:
                  name: sheet-apis-secret
                  key: zeroCacheServer
            - name: ZERO_CACHE_USER_ID
              value: "system:serviceaccount:$(POD_NAMESPACE):sheet-apis"
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: sheet-apis-secret
                  key: redisUrl
            - name: SHEET_AUTH_ISSUER
              valueFrom:
                secretKeyRef:
                  name: sheet-apis-secret
                  key: sheetAuthIssuer
            - name: SHEET_INGRESS_KUBERNETES_AUDIENCE
              value: sheet-apis
            - name: SHEET_INGRESS_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: sheet-apis-secret
                  key: sheetIngressBaseUrl
          livenessProbe:
            httpGet:
              path: /live
              port: sheet-apis-svc
              scheme: HTTP
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: sheet-apis-svc
              scheme: HTTP
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /live
              port: sheet-apis-svc
              scheme: HTTP
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 12
          volumeMounts:
            - name: sheet-apis-secret-path
              readOnly: true
              mountPath: "/.secret"
            - name: sheet-apis-tokens
              readOnly: true
              mountPath: "/var/run/secrets/tokens"
            - name: sheet-apis-ca-certificate
              readOnly: true
              mountPath: "/usr/local/share/ca-certificates/tiarastack"
      serviceAccountName: sheet-apis
      volumes:
        - name: sheet-apis-secret-path
          secret:
            secretName: sheet-apis-secret-path
        - name: sheet-apis-tokens
          projected:
            sources:
              - serviceAccountToken:
                  path: zero-cache-token
                  expirationSeconds: 3600
                  audience: zero-cache
              - serviceAccountToken:
                  path: sheet-auth-token
                  expirationSeconds: 3600
                  audience: sheet-auth
        - name: sheet-apis-ca-certificate
          configMap:
            name: sheet-apis-ca-certificate
            items:
              - key: ca-certificate.crt
                path: tiarastack.crt
---
apiVersion: v1
kind: Service
metadata:
  name: sheet-apis-service
  labels:
    app: sheet-apis
spec:
  ports:
    - name: sheet-apis-svc
      protocol: TCP
      port: 80
      targetPort: sheet-apis-svc
    - name: sheet-apis-met
      protocol: TCP
      port: 9464
      targetPort: sheet-apis-met
  selector:
    app: sheet-apis
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sheet-apis-service-ingress
spec:
  podSelector:
    matchLabels:
      app: sheet-apis
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: sheet-ingress-server
      ports:
        - protocol: TCP
          port: sheet-apis-svc
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: sheet-apis-met
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sheet-apis
  labels:
    app: sheet-apis
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: sheet-apis
  endpoints:
    - port: sheet-apis-met

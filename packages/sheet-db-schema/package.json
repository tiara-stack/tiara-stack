{
  "name": "sheet-db-schema",
  "version": "0.0.0",
  "private": true,
  "keywords": [],
  "author": "",
  "files": [
    "./dist/**"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    },
    "./models": {
      "types": "./dist/models.d.mts",
      "default": "./dist/models.mjs"
    },
    "./migrations": {
      "types": "./dist/migrations.d.mts",
      "default": "./dist/migrations.mjs"
    },
    "./testdb": {
      "types": "./dist/testdb.d.mts",
      "default": "./dist/testdb.mjs"
    },
    "./zero": {
      "types": "./dist/zero.d.mts",
      "default": "./dist/zero.mjs"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "format": "vp fmt --check",
    "format:apply": "vp fmt",
    "lint": "vp lint src",
    "pretest": "pnpm build",
    "test": "vp test run",
    "build": "pnpm --filter effect-sql-schema build && pnpm --filter effect-zero build && pnpm --filter typhoon-zero... build && pnpm exec effect-zero generate --config ./effect-zero.config.ts --output ./src/zero/schema.ts --skip-declare --format --force && vp pack",
    "schema:generate": "pnpm build && pnpm --filter effect-sql-kit build && pnpm exec effect-sql-kit generate",
    "schema:check:assertions": "node ./scripts/check-zero-schema-assertions.mjs",
    "schema:check:diff": "schema_changes=$(git status --short --untracked-files=all -- src/zero/schema.ts effect-sql-migrations) && test -z \"$schema_changes\" && pnpm schema:check:assertions",
    "schema:check:built": "pnpm exec effect-sql-kit generate && pnpm schema:check:diff",
    "schema:check": "pnpm schema:generate && pnpm schema:check:diff",
    "db:generate": "pnpm --filter effect-sql-schema build && pnpm --filter effect-zero build && pnpm --filter effect-sql-kit build && pnpm exec effect-sql-kit generate",
    "db:migrate": "pnpm --filter effect-sql-schema build && pnpm --filter effect-zero build && pnpm --filter effect-sql-kit build && pnpm exec effect-sql-kit migrate",
    "db:push": "pnpm --filter effect-sql-schema build && pnpm --filter effect-zero build && pnpm --filter effect-sql-kit build && pnpm exec effect-sql-kit push"
  },
  "dependencies": {
    "@rocicorp/zero": "^0.25.9",
    "effect-sql-kit": "workspace:*",
    "effect-sql-schema": "workspace:*",
    "effect-zero": "workspace:*",
    "typhoon-core": "workspace:*",
    "typhoon-zero": "workspace:*"
  },
  "devDependencies": {
    "@effect/platform-node": "catalog:",
    "@effect/vitest": "catalog:",
    "@electric-sql/pglite": "^0.5.4",
    "@types/node": "catalog:",
    "dotenv": "^17.2.3",
    "drizzle-orm": "0.45.1",
    "sheet-models": "workspace:*",
    "tooling-config": "workspace:*",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "effect": "catalog:",
    "sheet-models": "workspace:*"
  },
  "drizzleVersionPolicy": "Pin drizzle-orm to 0.45.1: 0.45.2 is incompatible with Zero 0.25.9 under exactOptionalPropertyTypes."
}

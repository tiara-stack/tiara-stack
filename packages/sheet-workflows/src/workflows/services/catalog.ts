import { ServicesDeliverStatus } from "sheet-workflow-contracts";

export const ServiceSheetWorkflowContracts = Object.freeze([ServicesDeliverStatus] as const);

export const serviceSheetWorkflowDefinitionVersion = "1";

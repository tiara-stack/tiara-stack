import { TeamsDeliverList } from "sheet-workflow-contracts";

export const TeamSheetWorkflowContracts = Object.freeze([TeamsDeliverList] as const);

export const teamSheetWorkflowDefinitionVersion = "1";
